/**
 * Base class for something that prints out a sequence of numbers.
 */
public abstract class SequencePrinter {
	/**
	 * Prints out the given numbers in this sequence.
	 *
	 * @param count Number of terms to print
	 */
	public abstract void printNumbers(int count);
}